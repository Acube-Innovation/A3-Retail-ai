"""Hook handlers for core ERPNext / HRMS doctypes.

Golden rule 1: core files are never edited. Everything that reacts to a core
document lives here and is wired through `doc_events` in hooks.py.
"""
